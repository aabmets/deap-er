# One-Plus-Lambda CMA

`StrategyOnePlusLambda` with `ea_generate_update` on `bm_sphere`.
The run prints the adapted σ (DEAP’s CMA plot, without matplotlib).
See the [Strategies](../../reference/strategies.md) reference.

```python
--8<-- "examples/evolution_strategies/cma_one_plus_lambda.py"
```
