# MAP-Elites

Illuminates a 2-D behavior grid of Rastrigin with `GridArchive` and
`ea_map_elites`. Fitness is on `ind.fitness`; the descriptor is the first
two genes.

```python
--8<-- "examples/genetic_algorithms/map_elites.py"
```
