# Genealogy

A short OneMax run that records a NetworkX-compatible genealogy.
`History.update` seeds the population; `history.decorator` wraps
`mate` and `mutate`. See
[Logging Statistics](../../tutorials/logging_statistics.md).

```python
--8<-- "examples/genetic_algorithms/history.py"
```
