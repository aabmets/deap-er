# Push GP

Two-level program search: prefix trees stay the laws, a private Push
policy reads summaries and emits toolbox actions (here, the next
lexicase exam). That is not a second public genome. See the
[Push GP tutorial](../../tutorials/push_gp.md) for the firewall
(`policy_observe`, `apply_policy_action`, held-out scoring, guards).

```python
--8<-- "examples/genetic_programming/push_gp.py"
```
