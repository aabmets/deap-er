# Informed Lexicase

Symbolic regression with one error per sample. Each generation rebuilds
an informed case subset and selects with ε-lexicase. Weighted primitives,
a `call_zero` terminal, and `tree_to_infix` are on the same script.

```python
--8<-- "examples/genetic_programming/lexicase.py"
```
